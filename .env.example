# Copy to .env.local (gitignored) for local development.
# Android emulator reaches the host machine at 10.0.2.2; a physical device needs the host's LAN IP.
# EAS builds read this from the EAS environment for the build profile instead.
EXPO_PUBLIC_API_URL=http://10.0.2.2:4000/api/v1
