// Stands in for zod/v4/locales/index.js (see metro.config.js). Zod's default English messages
// are imported directly by its schemas, so only the unused `z.locales.*` namespace shrinks to `en`.
export { default as en } from "zod/v4/locales/en.js";
